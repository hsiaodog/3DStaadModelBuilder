# Vite / Node
node_modules/
dist/
*.tsbuildinfo

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# Project artefacts (don't commit user data into repo)
*.stproj.json
*.xlsx
*.dxf
*.dwg
*.pdf
storage_host/

# Big binaries we explicitly committed in baseline shouldn't be re-added by accident
# pdf.min.js + pdf.worker.min.js are kept (vendored runtime)
